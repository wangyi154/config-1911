my:
  name: 王奕2
  age: 20
  gender: 男
spring:
  redis:
    database: 0
    host: localhost
    port: 6379
    timeout: 6000ms
    jedis:
      pool:
        max-active: 200
        max-wait: -1ms
        max-idle: 10
        min-idle: 0

  cloud:
    bus: 
      enabled: true
      trace: 
        enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"